<%= form_with(model: xroute, local: true) do |form| %>
  <% if xroute.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(xroute.errors.count, "error") %> prohibited this xroute from being saved:</h2>

      <ul>
      <% xroute.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="control-group">
    <%= form.label :hub_id %>
    <div class="controls">
      <%= collection_select( :xroute, :hub_id, Hub.all, :id, :name, {}, { :multiple => false } ) %>
    </div>
  </div>

  <div class="control-group">
    <%= form.label :city_id %>
    <div class="controls">
      <%= collection_select( :xroute, :city_id, City.all, :id, :name, {}, { :multiple => false } ) %>
    </div>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
